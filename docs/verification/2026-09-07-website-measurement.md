# Website measurement verification

September 7, 2026. This record covers the consent-gated website measurement release following the programmatic SEO release.

- `npm run verify`: build and all 190 tests passed; no skipped tests. Includes real SQL summary tests using temporary tables, existing public intake/notification tests and isolated browser-script consent tests.
- SEO build validator: 191 public pages, unique metadata, canonical origin/path, local links/anchors, parseable JSON-LD and sorted sitemap passed.
- Browser: privacy page inspected at desktop and 390px phone viewport. Phone document width and scroll width both 375px (remaining viewport width is the scrollbar).
- Isolated measurement fixture: real landing HTML and measurement assets with a synthetic ID, local origin and a CSP blocking third-party scripts/connections. No real Google collection occurred. Consent controls displayed at 390px and 1440px; No thanks hides the banner and Analytics choices reopens it. Desktop document width and scroll width both 1425px.
- Mobile inspection found footer navigation overflow; allowing link wrapping removed it. Consent dismissal now focuses the settings control without scrolling the visitor to the footer.
- Measurement configuration remains null. Google Analytics account creation is prepared at the terms agreement; Search Console URL-prefix ownership is prepared at the HTML-tag verification dialog. Owner approval is required to complete those external actions.

These checks do not prove Google collection, indexing, ranking, qualified lead volume, production inbox delivery or legal compliance. Production release availability is checked separately after deployment. No production inquiry or real email was generated by these tests.
